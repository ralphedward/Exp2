#include <iostream>
#include <conio.h>

using namespace std;

int main()
{
	int c, x = 0, y = 1, z;
	
	for (c = 0; c < 22; c++)
	{
		if (c <= 1)
		z = c;
		else
		{
			z = x + y;
			x = y;
			y = z;
		}
		cout << z << ", ";
	}
	
	_getch();
	return 0;
}
